# LTC-4 Z-Score Inference Sketch

This is a legacy Arduino sketch for LTC-4 inference with z-score normalization. For current deployment tests, prefer the sketches in:

```text
arduino/deployment_candidates/
```
