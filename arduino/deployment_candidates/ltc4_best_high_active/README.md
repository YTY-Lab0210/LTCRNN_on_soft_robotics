# LTC-4 High-Active Relay Candidate

This Arduino sketch uses LTC-4 inference and assumes the relay module is active when the control input is `HIGH`.

Use this version when the relay turns on with a HIGH input signal.
