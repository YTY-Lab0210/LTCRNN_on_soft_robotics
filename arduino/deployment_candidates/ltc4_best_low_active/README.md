# LTC-4 Low-Active Relay Candidate

This Arduino sketch uses LTC-4 inference and assumes the relay module is active when the control input is `LOW`.

Use this version when the relay turns on with a LOW input signal.
