# 1D-CNN Legacy Baseline

此資料夾保存早期 1D-CNN baseline 程式。1D-CNN 曾用於比較序列分類表現，但目前主線研究重點是 LTC-RNN、Vanilla RNN 與 LSTM。

## 檔案

| 檔案 / 資料夾 | 用途 |
| --- | --- |
| `CNN1D/` | 早期 1D-CNN 訓練與 ablation 腳本 |
| `draw_1d_cnn.py` | 將 1D-CNN 結果畫成圖 |

## 注意

此資料夾偏 legacy。若目前 paper 或簡報已移除 1D-CNN，可保留作為歷史比較，不建議當成主要實驗入口。
