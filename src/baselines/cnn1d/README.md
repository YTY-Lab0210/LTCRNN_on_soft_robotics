# 1D-CNN Baseline

This folder stores legacy 1D-CNN baseline scripts and architecture notes. The lightweight 1D-CNN design uses a compact convolutional structure with global average pooling to avoid parameter explosion.
