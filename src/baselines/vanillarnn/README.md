# Vanilla RNN Baseline

This folder stores Vanilla RNN / SimpleRNN baseline scripts. The current paper-style benchmark uses the integrated benchmark script in `src/ltc_bptt_5ch/`, while files here are retained as legacy or standalone references.
