# SimpleRNN Legacy Scripts

These scripts contain earlier SimpleRNN experiments, including single-model training, neuron-count sweeps, and reduced-training-set tests. Some paths may need to be updated before rerunning them on a new machine.
