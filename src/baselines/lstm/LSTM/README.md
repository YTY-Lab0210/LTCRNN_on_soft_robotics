# LSTM Legacy Scripts

These scripts include earlier LSTM experiments for single-model training, neuron-count sweeps, and reduced-training-set tests. Some local paths may need to be updated before rerunning.
